<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VeterinariaApp.Views.MenuVeterinarioPage"
             Title="Menú Veterinario"
             BackgroundColor="#F3F3F3">

    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="👨‍⚕️ Bienvenido Veterinario"
               Style="{StaticResource RoleTitleStyle}" />

        <Button x:Name="btnHistorial"
                Text="📋 Historial Médico"
                Style="{StaticResource VetButtonStyle}"
                Clicked="OnHistorialClicked" />

        <Button x:Name="btnAgenda"
                Text="📅 Agenda de Citas"
                Style="{StaticResource VetButtonStyle}"
                Clicked="OnAgendaClicked" />

        <Button x:Name="btnLista"
                Text="📖 Lista de Citas"
                Style="{StaticResource VetButtonStyle}"
                Clicked="OnListaCitasClicked" />

        <Button Text="🚪 Cerrar sesión"
                BackgroundColor="#F44336"
                TextColor="White"
                FontAttributes="Bold"
                Clicked="OnCerrarSesionClicked" />
    </VerticalStackLayout>
</ContentPage>
